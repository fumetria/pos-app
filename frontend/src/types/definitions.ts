export type ArticleLine = {
    id: string,
    name: string,
    details?: string,
    quantity: number,
    price: number,
    total: number
}

export type Article = {
    id: string;
    name: string;
    category: string;
    pvp: string;

}